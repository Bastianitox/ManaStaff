import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RecuperarpinPage } from './recuperarpin.page';

describe('RecuperarpinPage', () => {
  let component: RecuperarpinPage;
  let fixture: ComponentFixture<RecuperarpinPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RecuperarpinPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
